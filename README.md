# ai-saw

Local AI vs human text detection for PDF documents.

```bash
pip install -e .
ai-saw IEEE_Conference_Template-2.pdf
```

Reports are written to `reports/<pdf-name>/report.html` and `report.json`.
